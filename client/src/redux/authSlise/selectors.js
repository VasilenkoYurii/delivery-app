export const selectUser = state => state.auth.user;

export const selectIsLoggedIn = state => state.auth.isLoggedIn;

export const selectPromo = state => state.auth.promo;

export const selectToken = state => state.auth.token;
